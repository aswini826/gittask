                                                 Array.prototype.every()
                                                
   ASWINI A:    *This every() method is check whether atleast one item in the array fulfill a particular condition.
                *It return in Boolean Value.
                (eg):
                const numbers=[1,2,3,4];
                const test=numbers.every(isPositive);
                function isPositive(item) {
                return item < 0;
                }
                console.log(test);   //excepted output true
   
   SHERIN F:
   
   SOWMIYA MS:*The every() method tests whether all elements in the array pass the test implemented by the provided function. 
              *It returns a Boolean value.
              *It is an iterative function,it will execute untill the false value is present.
              *This function is called with arguments of the current element,the index of the current element and the array.
              (eg):
              function isBigEnough(element, index, array) {
              return element >= 10;
              }
              [12, 5, 8, 130, 44].every(isBigEnough);
              //expected output false
   
   
   ASHMITHA S
   
   
                                               Array.prototype.fill()
   ASWINI A:
   
   SHERIN F:
   
   SOWMIYA MS:
   
   ASHMITHA S                                               
                                               Array.prototype.filter()
                                               
                                               
   ASWINI A:
   
   SHERIN F:It will filter the values from an array
   
   SOWMIYA MS:*The filter() method creates a new array filled with elements that pass a test provided by a function.
              *The filter() method is an iterative method. 
              *The filter() method is a copying method. 
              *It does not alter this but instead returns a shallow copy that contains the same elements as the ones from the original array.
              (eg):
              function isBigEnough(value) {
              return value >= 10;
              }

              const filtered = [12, 5, 8, 130, 44].filter(isBigEnough);
              // filtered is [12, 130, 44]
   
   ASHMITHA S                                               
                                               Array.prototype.find()
                                               
                                               
    ASWINI A:
   
   SHERIN F:
   
   SOWMIYA MS:
   
   ASHMITHA S                                            
